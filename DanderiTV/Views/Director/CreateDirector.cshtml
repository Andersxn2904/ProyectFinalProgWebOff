@using DanderiTV.Layer.Application.Models.Director
@model SaveDirectorModel;
@{
    bool editMode = Model.ID == 0 ? false : true;
    ViewData["Title"] = "Save Producer";
}

<div class="container">
    <div class="col-6 offset-3">
        <h1>
            @(editMode ? $"Edit producer: {Model.DirectorName}" : "New Director")
        </h1>
        <form class="needs-validation" method="post" asp-controller="Director" asp-action="@(editMode ? "Edit" : "Create")" novalidate="">
            <div class="row g-3">
                <div class="col-12">
                    <label asp-for="DirectorName" class="form-label">Name</label>
                    <input type="text" asp-for="DirectorName" class="form-control" id="Name" placeholder="" required="">
                    <span asp-validation-for="DirectorName" class="text-danger"></span>
                </div>
            </div>
            <div class="row g-3">
                <div class="col-12">
                    <label asp-for="Lastname" class="form-label">Lastname</label>
                    <input type="text" asp-for="Lastname" class="form-control" id="Name" placeholder="" required="">
                    <span asp-validation-for="Lastname" class="text-danger"></span>
                </div>
            </div>
            <input type="hidden" asp-for="ID" />
            <div class="mt-3">
                <button class="btn btn-success" type="submit">Save</button>
                <a asp-controller="Director" asp-action="Index" class="btn btn-warning">Go back</a>
            </div>
        </form>
    </div>


</div>


